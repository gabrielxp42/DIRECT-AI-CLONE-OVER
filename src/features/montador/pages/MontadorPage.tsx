import React from 'react';
import MontadorInterface from '@/features/montador/components/MontadorInterface';

const MontadorPage: React.FC = () => {
  return (
    <div className="w-full h-full bg-[#050505] overflow-hidden">
      <MontadorInterface />
    </div>
  );
};

export default MontadorPage;
