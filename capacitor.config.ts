import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.merryshibaplay',
  appName: 'merry-shiba-play',
  webDir: 'dist'
};

export default config;
